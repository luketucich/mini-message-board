<ul>
  <% messages.forEach(message => { %>
  <li class="message">
    <p class="user"><%= message.user %>:</p>
    <p><%= message.text %></p>
    <p class="date"><%= message.added.toLocaleString() %></p>
  </li>
  <% }); %>
</ul>
